package com.epam.workshop

case class RawAnswer(commentCount: String,
                     creationDate: String,
                     id: String,
                     ownerUserId: String,
                     parentId: String,
                     postTypeId: String,
                     score: String)
